#include<stdio.h>

int main(){
    char st [30];
    gets(st);

    printf("%s",st);

    //  puts(st); // Prints the string & places the cursor on the next line  

     printf("nice");

    return 0;
}